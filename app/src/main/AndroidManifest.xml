<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebaseproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireBaseProject">
        <activity android:name=".fragments.FragmentContainer"></activity>
        <activity android:name=".Upload_Retrive.UploadItem"/>
        <activity android:name=".authfiles.forgotPasswordActivity" />
        <activity android:name=".authfiles.changePasswordActivity" />
        <activity android:name=".authfiles.SignInActivity" />

        <activity android:name=".authfiles.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>